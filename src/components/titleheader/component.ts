import {Component, Input} from '@angular/core';

@Component
({
    moduleId: module.id,
    selector: 'mikan-control-title-header',
    templateUrl: 'template.html'
})

export class ComponentTitleHeader
{
    constructor()
	{
    };
}