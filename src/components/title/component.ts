import {Component, Input} from '@angular/core';

@Component
({
    moduleId: module.id,
    selector: 'mikan-control-title',
    templateUrl: 'template.html'
})

export class ComponentTitle
{
    constructor()
	{
    };
}