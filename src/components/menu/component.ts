import {Component, Input} from '@angular/core';

@Component
({
    moduleId: module.id,
    selector: 'mikan-control-menu',
    templateUrl: 'template.html'
})

export class ComponentMenu
{
    constructor()
	{
    };
}