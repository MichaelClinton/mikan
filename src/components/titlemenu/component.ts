import {Component, Input} from '@angular/core';

@Component
({
    moduleId: module.id,
    selector: 'mikan-control-title-menu',
    templateUrl: 'template.html'
})

export class ComponentTitleMenu
{
    constructor()
	{
    };
}